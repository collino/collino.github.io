{{> header}}
	    <div class="main-wrapper">
	    	<h1>{{title}}</h1>
			{{{ contents }}}
			{{#each collections.projects}}
            <figure>
                <figcaption>
                    <h3>{{title}}</h3>
                    <span>{{date}}</span>
                <figcaption>
                <img src="{{image}}">
            </figure>
        	{{/each}}
		</div>
{{> footer}}